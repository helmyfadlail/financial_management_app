import { Suspense } from "react";
import { ResetPassword } from "@/layouts";

export default function ResetPasswordPage() {
  <Suspense fallback={<div>Loading...</div>}>
    return <ResetPassword />;
  </Suspense>;
}
